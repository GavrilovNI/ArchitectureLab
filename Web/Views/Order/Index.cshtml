@using Web.Data.Models
@model IEnumerable<BoughtCart>

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<div class="row mt-5 mb-2">
	@{
		foreach(BoughtCart boughtCart in Model.Reverse()) {
			@Html.Partial("_BoughtCartPartial", boughtCart);
			<hr>
		}
	}
</div>
