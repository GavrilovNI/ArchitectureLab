@using Web.Data.Models
@model IEnumerable<ProductBill>

@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
	foreach(ProductBill productBill in Model) {
		@Html.Partial("_ProductBillPartial", productBill);
		<hr/>
	}
}